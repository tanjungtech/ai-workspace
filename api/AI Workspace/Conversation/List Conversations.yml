info:
  name: List Conversations
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/conversations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
