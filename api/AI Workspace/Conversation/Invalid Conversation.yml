info:
  name: Invalid Conversation
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/conversations/999"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
