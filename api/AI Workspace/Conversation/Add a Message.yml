info:
  name: Add a Message
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/conversations/{{CONVERSATION_ID}}/messages"
  body:
    type: json
    data: |-
      {
          "role":"user",
          "content":"Explain JWT."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
