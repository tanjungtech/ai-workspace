info:
  name: Coding Prompt
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/chat"
  body:
    type: json
    data: |-
      {
        "conversationId": "demo",
        "assistantType": "coding",
        "message": "Explain JWT authentication"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
