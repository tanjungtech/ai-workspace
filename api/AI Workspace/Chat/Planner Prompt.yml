info:
  name: Planner Prompt
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/chat"
  body:
    type: json
    data: |-
      {
        "conversationId": "demo",
        "assistantType": "planner",
        "message": "Explain JWT authentication"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
