info:
  name: General Prompt
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/chat"
  body:
    type: json
    data: |-
      {
        "conversationId": "demo",
        "assistantType": "general",
        "message": "Explain JWT authentication"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
