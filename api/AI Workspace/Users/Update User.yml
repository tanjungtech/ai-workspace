info:
  name: Update User
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/users/1"
  body:
    type: json
    data: |-
      {
        "name": "John Updated"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
