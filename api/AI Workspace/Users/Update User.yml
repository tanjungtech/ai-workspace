info:
  name: Update User
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/api/user/2"
  body:
    type: json
    data: |-
      {
        "name": "John Updated",
        "email": "john@example.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
