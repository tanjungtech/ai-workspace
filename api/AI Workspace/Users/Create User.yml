info:
  name: Create User
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/api/user"
  body:
    type: json
    data: |-
      {
        "name": "Jane Doe",
        "email": "john@example.com",
        "password": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
